name: Génération & déploiement calendrier FR

on:
  workflow_dispatch:
  schedule:
  - cron: "45 3 * * *"    # 06h00 Paris (Maj & notif prête entre 05h45 a 06h00)
  - cron: "45 9 * * *"    # 12h00 Paris (Maj & notif prête entre 11h45 a 12h00)
  - cron: "45 15 * * *"   # 18h00 Paris (Maj & notif prête entre 17h45 a 18h00)
  - cron: "45 21 * * *"   # 00h01 Paris (Maj & notif prête entre 23h45 a 00h01)


jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout du dépôt
      - uses: actions/checkout@v4

      # 2️⃣ Installer Python 3.11
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3️⃣ Installer les dépendances Python
      - name: Installer les dépendances
        run: |
          python -m pip install --upgrade pip --quiet
          pip install --upgrade -r requirements.txt --quiet

      # 4️⃣ Créer le dossier calendar si inexistant
      - name: Créer dossier calendar
        run: mkdir -p calendar

      # 5️⃣ Générer le calendrier FR
      - name: Générer le calendrier FR
        id: generate
        run: |
          set -e  # stoppe le workflow si erreur
          python scripts/translate_ics.py

      # 6️⃣ Commit et push calendrier FR
      - name: Commit & push calendrier
        if: steps.generate.conclusion == 'success'
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add calendar/gocal_fr.ics
          git diff-index --quiet HEAD || git commit -m "MàJ calendrier FR"
          git pull --rebase origin main || echo "Pas de rebase nécessaire"
          git push origin main

      # 7️⃣ Déploiement GitHub Pages
      - name: Déploiement GitHub Pages
        if: steps.generate.conclusion == 'success'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./  # racine du repo
          publish_branch: gh-pages
          force_orphan: true

      # 8️⃣ Notification Pushover succès
      - name: Notification Pushover succès
        if: steps.generate.conclusion == 'success'
        env:
          PUSHOVER_TOKEN: ${{ secrets.PUSHOVER_TOKEN }}
          PUSHOVER_USER: ${{ secrets.PUSHOVER_USER }}
        run: |
          curl -s \
            --form-string "token=${PUSHOVER_TOKEN}" \
            --form-string "user=${PUSHOVER_USER}" \
            --form-string "message=📅 Calendrier PoGo FR → Auto M.A.J réussie ! ✅" \
            https://api.pushover.net/1/messages.json

      # 9️⃣ Notification Pushover erreur
      - name: Notification Pushover erreur
        if: failure()
        env:
          PUSHOVER_TOKEN: ${{ secrets.PUSHOVER_TOKEN }}
          PUSHOVER_USER: ${{ secrets.PUSHOVER_USER }}
        run: |
          curl -s \
            --form-string "token=${PUSHOVER_TOKEN}" \
            --form-string "user=${PUSHOVER_USER}" \
            --form-string "message=⚠️ Erreur lors de la génération du calendrier PoGo FR !" \
            https://api.pushover.net/1/messages.json
