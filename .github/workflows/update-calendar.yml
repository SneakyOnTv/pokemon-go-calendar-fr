name: Génération & déploiement calendrier FR

on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * *"    # 06h00 Paris
    - cron: "0 10 * * *"   # 12h00 Paris
    - cron: "0 16 * * *"   # 18h00 Paris
    - cron: "1 22 * * *"   # 00h01 Paris

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout du dépôt
      - uses: actions/checkout@v4

      # 2️⃣ Installer Python 3.11
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3️⃣ Installer les dépendances Python
      - name: Installer les dépendances
        run: |
          python -m pip install --upgrade pip --quiet
          pip install --upgrade -r requirements.txt --quiet

      # 4️⃣ Créer le dossier calendar si inexistant
      - run: mkdir -p calendar

      # 5️⃣ Générer le calendrier FR
      - name: Générer le calendrier FR
        run: python scripts/translate_ics.py

      # 6️⃣ Commit et push avec gestion simple des conflits
      - name: Commit et push calendrier
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add calendar/gocal_fr.ics
          git commit -m "MàJ calendrier FR" || echo "Rien à commit"
          git pull --rebase origin main || echo "Pas de rebase nécessaire"
          git push origin main

      # 7️⃣ Déploiement GitHub Pages (si index.html ou page HTML existe)
      - name: Déploiement GitHub Pages
        uses: peaceiris/actions-gh-pages@v6
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./  # racine du repo contenant index.html
          publish_branch: gh-pages
          force_orphan: true

      # 8️⃣ Notification dans les logs GitHub
      - name: Notification de succès dans GitHub
        if: success()
        run: echo "::notice title=Calendrier FR::✅ Auto MàJ calendrier FR réussie !"

      # 9️⃣ Notification Pushover
      - name: Notification push Pushover
        if: success()
        env:
          PUSHOVER_TOKEN: ${{ secrets.PUSHOVER_TOKEN }}
          PUSHOVER_USER: ${{ secrets.PUSHOVER_USER }}
        run: |
          curl -s \
            --form-string "token=${PUSHOVER_TOKEN}" \
            --form-string "user=${PUSHOVER_USER}" \
            --form-string "message=📅Calendrier PoGo FR → Auto M.A.J réussie ! ✅" \
            https://api.pushover.net/1/messages.json
